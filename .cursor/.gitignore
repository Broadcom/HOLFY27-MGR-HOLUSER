# Cursor IDE session/transient data - do NOT track
projects/
plugins/
skills-cursor/
commands/
plans/
subagents/
blocklist
unified_repo_list.json
ide_state.json

# Only track these (the negations below):
#   skills/         - Custom agent skills (vcf-9-api, vcf-troubleshooting)
#   rules/          - Workspace rules (.mdc files)
#   mcp.json        - MCP server configuration
